Try to understanding why the scaling isn't working for the port side. Seems like we're somehow scaling wrong. There is this formula:
```matlab
((input - old min) / (old max - old min)) * ((new max - new min) + new min)
```
but I'm not sure what the 'old' values are. Since we're pulling the nav out of our asses,  we don't necessarily have 'old' values, although because we're setting the coords to count from 0 at the outset, that's likely the old min. The script was using the highest frame number as the old max, but that isn't really something that means anything in the context of this operation; we need to figure out what the old max is. Maybe max X?
